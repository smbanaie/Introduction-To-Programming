# کدگذاری کاراکتر: چگونه متن دیجیتال می‌شود

## مشکل ذخیره‌سازی متن

کامپیوترها با اعداد کار می‌کنند، اما انسان‌ها با متن ارتباط برقرار می‌کنند. کدگذاری کاراکتر این شکاف را پر می‌کند با تخصیص کدهای عددی به حروف، نمادها و کاراکترها.

## ASCII: پایه و اساس

### ASCII چیست؟
کد استاندارد آمریکایی برای تبادل اطلاعات - استاندارد اصلی کدگذاری کاراکتر.

- **ایجاد شده**: دهه ۱۹۶۰ برای ارتباط دستگاه‌های تلگراف
- **اندازه**: ۷ بیت (۱۲۸ کاراکتر)
- **پوشش**: حروف انگلیسی، اعداد، نمادهای پایه

### جدول ASCII
```
۳۲-۴۷:  !"#$%&'()*+,-./
۴۸-۵۷: ۰۱۲۳۴۵۶۷۸۹
۶۵-۹۰: ABCDEFGHIJKLMNOPQRSTUVWXYZ
۹۷-۱۲۲: abcdefghijklmnopqrstuvwxyz
```

### ASCII در باینری
```
'A' = ۰۱۰۰۰۰۰۱ (اعشار ۶۵)
'B' = ۰۱۰۰۰۰۱۰ (اعشار ۶۶)
'Hello' = ۰۱۰۰۱۰۰۰ ۰۱۱۰۰۱۰۱ ۰۱۱۰۱۱۰۰ ۰۱۱۰۱۱۰۰ ۰۱۱۰۱۱۱۱
```

## ASCII توسعه‌یافته

### ASCII ۸ بیتی
- **محدوده توسعه‌یافته**: ۱۲۸-۲۵۵ (۱۲۸ کاراکتر اضافی)
- **سازگاری به عقب**: ۱۲۸ کاراکتر اول همان ۷ بیت ASCII
- **مشکل**: چندین کد صفحه متفاوت برای زبان‌های مختلف

### سردرگمی کد صفحه
مناطق مختلف از انواع ASCII توسعه‌یافته متفاوتی استفاده کردند:
- **CP1252**: اروپای غربی (ویندوز)
- **ISO-8859-1**: اروپای غربی (لینوکس/یونیکس)
- **KOI8-R**: سیریلیک (روسی)

## یونیکد: راه‌حل جهانی

### اهداف یونیکد
- **جهانی**: نمایش هر کاراکتر در هر زبان
- **منحصر به فرد**: هر کاراکتر یک نقطه کد دارد
- **سازگار**: کد یکسان برای کاراکتر یکسان در همه جا

### نقاط کد یونیکد
- **نشان‌گذاری**: U+ به علاوه عدد هگزادسیمال
- **محدوده**: U+0000 تا U+10FFFF (۱,۱۱۴,۱۱۲ کاراکتر ممکن)
- **مثال‌ها**:
  - U+0041: حرف بزرگ لاتین A
  - U+0627: حرف عربی الف
  - U+1F600: ایموجی صورت خندان 😀

## UTF-8: یونیکد در بایت‌ها

### کدگذاری طول متغیر
UTF-8 از ۱-۴ بایت در هر کاراکتر استفاده می‌کند:
- **۱ بایت**: ASCII استاندارد (U+0000 تا U+007F)
- **۲ بایت**: لاتین، یونانی، عربی (U+0080 تا U+07FF)
- **۳ بایت**: چندزبانه پایه (U+0800 تا U+FFFF)
- **۴ بایت**: کاراکترهای نادر، ایموجی‌ها (U+10000 تا U+10FFFF)

### مزایای UTF-8
- **سازگار با ASCII**: فایل‌های ASCII موجود بدون تغییر کار می‌کنند
- **کارآمد فضایی**: کاراکترهای رایج از بایت‌های کمتری استفاده می‌کنند
- **خود همگام**: می‌توان مرزهای کاراکتر را در هر جایی پیدا کرد

### مثال‌های UTF-8
```
A:     ۰۱۰۰۰۰۰۱ (۱ بایت، همان ASCII)
á:     ۱۱۰۰۰۰۱۱ ۱۰۱۰۰۰۰۱ (۲ بایت)
中:   ۱۱۱۰۰۱۰۰ ۱۰۱۱۱۰۰۰ ۱۰۱۰۱۱۰۱ (۳ بایت)
😀:  ۱۱۱۱۰۰۰۰ ۱۰۰۱۱۱۱۱ ۱۰۰۱۱۰۰۰ ۱۰۰۰۰۰۰۰ (۴ بایت)
```

## کدگذاری‌های یونیکد دیگر

### UTF-16
- **طول ثابت/متغیر**: ۲ یا ۴ بایت در هر کاراکتر
- **BOM**: نشان بایت برای تشخیص endianness
- **استفاده**: داخلی ویندوز، رشته‌های جاوا

### UTF-32
- **اندازه ثابت**: ۴ بایت در هر کاراکتر
- **ساده**: نگاشت مستقیم از نقاط کد
- **پر مصرف**: ۴ بایت حتی برای کاراکترهای ASCII

## تشخیص کدگذاری

### نشان ترتیب بایت (BOM)
بایت‌های ویژه در ابتدای فایل:
- **UTF-8**: EF BB BF
- **UTF-16 BE**: FE FF
- **UTF-16 LE**: FF FE
- **UTF-32 BE**: 00 00 FE FF

### تحلیل محتوا
- **روش‌های آماری**: تحلیل فراوانی کاراکترها
- **اکتشافی**: تطبیق الگو برای امضای کدگذاری

## برنامه‌نویسی با کدگذاری‌ها

### مثال‌های کدگذاری Python
```python
# کدگذاری رشته به بایت‌ها
text = "سلام، دنیا"
utf8_bytes = text.encode('utf-8')
print(utf8_bytes)  # b'\xd8\xb3\xd9\x84\xd8\xa7\xd9\x85, \xd8\xaf\xd9\x86\xdb\x8c\xd8\xa7'

# رمزگشایی بایت‌ها به رشته
decoded = utf8_bytes.decode('utf-8')
print(decoded)  # "سلام، دنیا"

# مدیریت خطاهای کدگذاری
try:
    bad_bytes.decode('ascii')
except UnicodeDecodeError:
    print("نمی‌توان با ASCII رمزگشایی کرد")
```

### ورودی/خروجی فایل با کدگذاری
```python
# نوشتن با کدگذاری مشخص
with open('file.txt', 'w', encoding='utf-8') as f:
    f.write("سلام، دنیا")

# خواندن با تشخیص کدگذاری
with open('file.txt', 'r', encoding='utf-8') as f:
    content = f.read()
```

## مشکلات رایج کدگذاری

### موجی‌باک (متن مخدوش)
وقتی از کدگذاری اشتباه برای رمزگشایی استفاده شود:
```
اصلی: "café" (UTF-8: c3 a9)
رمزگشایی اشتباه (Latin-1): "cÃ©"
```

### فساد کاراکتر
شکستن توالی‌های چند بایتی:
```
"café" UTF-8: 63 61 66 c3 a9
Latin-1: Ã © (دو کاراکتر)
```

### مشکلات برش
بریدن کاراکترهای چند بایتی:
```
"café" در UTF-8: 63 61 66 c3 a9
برش در ۴ بایت: 63 61 66 c3 → "cafÃ" (نامعتبر)
```

## عادی‌سازی

### معادلات یونیکد
توالی‌های بایتی مختلف برای نمایش بصری یکسان:
- **NFC**: ترکیب شده (تک نقطه کد در صورت امکان)
- **NFD**: تجزیه شده (کاراکترهای پایه + ترکیب جداگانه)
- **NFKC/NFKD**: اشکال سازگاری

### مثال
```
é می‌تواند نمایش داده شود به عنوان:
- نقطه کد تک: U+00E9 (NFC)
- پایه + ترکیب: U+0065 U+0301 (NFD)
```

## ملاحظات پردازش متن

### طول رشته
```python
text = "café"
print(len(text))  # ۴ کاراکتر
print(len(text.encode('utf-8')))  # ۵ بایت
```

### عملیات زیررشته
```python
text = "café"
# مراقب برش رشته‌های چند بایتی باشید
print(text[3])  # 'é'
print(text[3:4])  # 'é'
```

### عبارات منظم
```python
import re
text = "café résumé naïve"
# تطبیق آگاه از یونیکد
matches = re.findall(r'\w+', text, re.UNICODE)
```

## بین‌المللی‌سازی (I18N)

### پشتیبانی محلی
- **کدهای زبان**: en, es, zh, ar
- **کدهای کشور**: US, MX, CN, SA
- **کدهای خط**: Latn, Cyrl, Hans

### جهت متن
- **LTR**: چپ به راست (انگلیسی، اسپانیایی)
- **RTL**: راست به چپ (عربی، عبری، پارسی)
- **BiDi**: متن دو جهته مخلوط

### قالب‌بندی فرهنگی
- **اعداد**: ۱,۲۳۴.۵۶ در برابر ۱.۲۳۴,۵۶
- **تاریخ‌ها**: MM/DD/YYYY در برابر DD/MM/YYYY
- **ارز**: $ در برابر € در برابر ¥

## روش‌های بهتر

### همیشه کدگذاری را مشخص کنید
```python
# خوب
with open('file.txt', 'r', encoding='utf-8') as f:
    text = f.read()

# بد - به سیستم وابسته است
with open('file.txt', 'r') as f:
    text = f.read()
```

### از رشته‌های یونیکد استفاده کنید
```python
# Python 3: رشته‌ها به طور پیش‌فرض یونیکد هستند
text = "Hello, 世界, سلام"  # رشته یونیکد

# در صورت نیاز به بایت تبدیل کنید
data = text.encode('utf-8')
```

### خطاها را به زیبایی مدیریت کنید
```python
try:
    with open('file.txt', 'r', encoding='utf-8') as f:
        text = f.read()
except UnicodeDecodeError:
    print("مشکل کدگذاری فایل")
    # کدگذاری دیگر را امتحان کنید یا خطا را مدیریت کنید
```

## نکات کلیدی

۱. **ASCII نقطه شروع بود**: ۱۲۸ کاراکتر برای انگلیسی پایه
۲. **یونیکد جهانی بودن را فراهم می‌کند**: هر کاراکتر در هر زبان
۳. **UTF-8 استاندارد است**: کارآمد، سازگار به عقب، غالب در وب
۴. **کدگذاری مهم است**: کدگذاری اشتباه منجر به متن مخدوش می‌شود
۵. **همیشه کدگذاری را مشخص کنید**: از مشکلات وابسته به پلتفرم جلوگیری می‌کند

## مطالعه بیشتر
- مستندات استاندارد یونیکد را مطالعه کنید
- کتابخانه‌های بین‌المللی‌سازی را بیاموزید
- الگوریتم‌های پردازش متن را کاوش کنید
- چینش و مرتب‌سازی برای زبان‌های مختلف را درک کنید