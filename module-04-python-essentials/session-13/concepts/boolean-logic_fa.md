# منطق بولی: درک درست و نادرست

## چه چیزهایی یاد خواهید گرفت
- True و False در پایتون یعنی چه
- چگونه شرایط را با and، or، not ترکیب کنیم
- چگونه از عملگرهای مقایسه استفاده کنیم
- اشتباهات رایج مبتدی‌ها

---

## منطق بولی چیست؟

منطق بولی درباره **True** (درست) و **False** (نادرست) است. این پایه و اساس تمام تصمیم‌ها در برنامه‌نویسی است.

مثل کلید روشنایی در نظر بگیرید:
- **True** = روشن (بله، چراغ روشن است)
- **False** = خاموش (خیر، چراغ خاموش است)

```
در زندگی واقعی:       در پایتون:
«آیا در باز است؟»     door_open = True
«آیا باران می‌بارد؟»   is_raining = False
«آیا ۱۸ سال یا       age >= 18 → True یا False
بیشتر داری؟»         
```

---

## مقادیر بولی در پایتون

### True و False

```python
# اینها تنها دو مقدار بولی هستند
is_sunny = True
is_raining = False

print(is_sunny)   # نشان می‌دهد: True
print(is_raining)  # نشان می‌دهد: False

# بررسی نوع
print(type(True))   # نشان می‌دهد: <class 'bool'>
print(type(False))  # نشان می‌دهد: <class 'bool'>
```

### ایجاد بولی از مقایسه‌ها

```python
age = 25

# عملگرهای مقایسه True/False ایجاد می‌کنند
is_adult = age >= 18      # True
is_senior = age >= 65     # False
is_teen = age < 20        # False

print(f"بزرگسال است: {is_adult}")
print(f"سالمند است: {is_senior}")
```

---

## عملگرهای مقایسه: ایجاد درست/نادرست

این عملگرها دو چیز را مقایسه می‌کنند و `True` یا `False` به شما می‌دهند.

| عملگر | معنی | مثال | نتیجه |
|--------|------|------|-------|
| `==` | برابر با | `5 == 5` | `True` |
| `!=` | نابرابر با | `5 != 3` | `True` |
| `>` | بزرگتر از | `7 > 5` | `True` |
| `<` | کوچکتر از | `3 < 5` | `True` |
| `>=` | بزرگتر یا مساوی | `5 >= 5` | `True` |
| `<=` | کوچکتر یا مساوی | `4 <= 7` | `True` |

### مثال‌ها

```python
# برابر با (==)
print(5 == 5)        # True
print(5 == 6)        # False

# نابرابر با (!=)
print(5 != 6)        # True
print(5 != 5)        # False

# بزرگتر از (>)
print(7 > 5)         # True
print(5 > 7)         # False

# کوچکتر از (<)
print(3 < 5)         # True
print(8 < 3)         # False

# بزرگتر یا مساوی (>=)
print(5 >= 5)        # True (مساوی هم حساب می‌شود!)
print(7 >= 5)        # True

# کوچکتر یا مساوی (<=)
print(4 <= 4)        # True (مساوی هم حساب می‌شود!)
print(4 <= 6)        # True
```

---

## عملگرهای منطقی: ترکیب شرایط

### AND - هردو باید درست باشند

مثل نیاز به **دو کلید** برای باز کردن گاوصندوق:

```python
has_key1 = True
has_key2 = True

if has_key1 and has_key2:
    print("گاوصندوق باز شد!")
# هردو کلید لازم است!
```

**جدول درستی AND:**
```
True  AND True   → True ✓ (هردو هستند)
True  AND False  → False ✗ (کلید ۲ کم است)
False AND True   → False ✗ (کلید ۱ کم است)
False AND False  → False ✗ (هردو کم هستند)
```

### OR - حداقل یکی باید درست باشد

مثل داشتن **دو راه** برای ورود به ساختمان:

```python
has_key = False
knows_password = True

if has_key or knows_password:
    print("در باز شد!")
# هر کدام کار می‌کند!
```

**جدول درستی OR:**
```
True  OR True   → True ✓ (هردو کار می‌کنند)
True  OR False  → True ✓ (کلید کار می‌کند)
False OR True   → True ✓ (رمز کار می‌کند)
False OR False  → False ✗ (هیچ‌کدام کار نمی‌کنند)
```

### NOT - معکوس می‌کند

مثل **کلید برعکس** که وضعیت را برعکس می‌کند:

```python
is_dark = True

if not is_dark:
    print("خارج روشن است!")
# not True می‌شود False

is_dark = False

if not is_dark:
    print("خارج روشن است!")
# not False می‌شود True، پس این چاپ می‌شود!
```

**جدول درستی NOT:**
```
NOT True  → False
NOT False → True
```

---

## نمودار ASCII: نحوه کار منطق

```
مثال: آیا می‌توانی وارد کلوپ شوی؟

        ┌──────────────────────┐
        │   age >= 18؟         │
        │   (آیا کلید         │
        │    اول را داری؟)      │
        └──────────┬───────────┘
                   │
              بله  ▼  خیر
        ┌────────────┐
        │  has_id؟   │─────→ نمی‌توانی وارد شوی
        │  (کلید     │        (کلید سن
        │   دوم؟)    │        کم است)
        └─────┬──────┘
              │
        ┌─────┴─────┐
     بله│           │خیر
        ▼           ▼
┌──────────────┐ ┌──────────────┐
│ می‌توانی      │ │ نمی‌توانی    │
│ وارد شوی!   │ │ وارد شوی    │
│ ✓✓          │ │ (کلید شناسه │
│              │ │ کم است)     │
└──────────────┘ └──────────────┘
```

---

## مثال‌های کاربردی

### سیستم کنترل دسترسی

```python
age = 25
has_id = True
is_member = False

# باید بزرگسال باشی AND شناسه داشته باشی
if age >= 18 and has_id:
    print("✅ می‌توانی وارد کلوپ شوی")
else:
    print("❌ نمی‌توانی وارد شوی")

# اعضا تخفیف می‌گیرند OR سالمندان
if is_member or age >= 65:
    print("🎉 تخفیف اعمال شد!")
```

### اعتبارسنجی ورودی کاربر

```python
username = input("نام کاربری: ")
password = input("رمز عبور: ")

# بررسی اینکه هردو خالی نیستند
if username and password:
    print("در حال پردازش ورود...")
else:
    if not username:
        print("❌ نام کاربری لازم است")
    if not password:
        print("❌ رمز عبور لازم است")
```

### هشدارهای دما

```python
temp = 95
is_humid = True

# گرم AND مرطوب خطرناک است
if temp > 90 and is_humid:
    print("⚠️ هشدار گرما! آب بنوش!")
# فقط گرم
elif temp > 90:
    print("🔥 هوا گرم است!")
# فقط مرطوب
elif is_humid:
    print("💧 هوا مرطوب است")
else:
    print("✅ هوای خوبی است")
```

---

## اشتباهات رایج مبتدی‌ها

### اشتباه ۱: استفاده از = به جای ==

```python
# ❌ اشتباه - انتساب می‌دهد، مقایسه نمی‌کند
if x = 5:
    print("x برابر ۵ است")
# خطا! خطای نحوی

# ✅ درست
if x == 5:
    print("x برابر ۵ است")
```

### اشتباه ۲: اشتباه گرفتن AND با OR

```python
# ❌ اشتباه - استفاده از OR وقتی AND لازم است
if age >= 18 or has_license:
    print("می‌تواند رانندگی کند")
# این به هر ۱۸+ اجازه رانندگی بدون گواهینامه می‌دهد!

# ✅ درست
if age >= 18 and has_license:
    print("می‌تواند رانندگی کند")
```

### اشتباه ۳: نقیض مضاعف (گیج‌کننده!)

```python
# ❌ گیج‌کننده - سخت برای فهم
if not (not is_valid):
    print("معتبر")
# not (not True) = True

# ✅ ساده
if is_valid:
    print("معتبر")
```

### اشتباه ۴: بررسی True به روش اشتباه

```python
# ❌ اشتباه - مقایسه با True اضافی است
if is_valid == True:
    print("معتبر")

# ✅ درست
if is_valid:
    print("معتبر")
```

---

## خودتان امتحان کنید: تمرین‌ها

### تمرین ۱: صلاحیت رأی

بررسی اینکه آیا کسی می‌تواند رأی بدهد (۱۸+ AND شهروند).

```python
age = int(input("سن خود را وارد کن: "))
is_citizen = input("آیا شهروند هستید؟ (بله/خیر): ") == "بله"

if age >= 18 and is_citizen:
    print("✅ شما واجد شرایط رأی دادن هستید!")
elif age < 18:
    print(f"⏳ {18 - age} سال دیگر صبر کنید.")
else:
    print("❌ برای رأی دادن شهروندی لازم است.")
```

### تمرین ۲: دسترسی به بازی

بررسی دسترسی به بازی (بزرگسال OR رضایت والدین).

```python
age = int(input("سن شما: "))
has_consent = input("رضایت والدین؟ (بله/خیر): ") == "بله"
is_adult = age >= 18

if is_adult or has_consent:
    print("🎮 دسترسی به بازی مجاز است!")
else:
    print("🔒 دسترسی به بازی رد شد. نیاز به بزرگسال یا رضایت.")
```

### تمرین ۳: رفع منطق

این سیستم ورود باگ دارد. آنها را رفع کنید:

```python
# کد با اشکال
username = input("نام کاربری: ")
password = input("رمز عبور: ")
is_active = True

if username == "admin" and password = "secret":
    print("خوش آمدید!")
elif not is_active:
    print("حساب غیرفعال است")
```

<details>
<summary>برای دیدن پاسخ کلیک کنید</summary>

```python
# کد رفع شده
username = input("نام کاربری: ")
password = input("رمز عبور: ")
is_active = True

# اول بررسی کن حساب فعال است
if not is_active:
    print("حساب غیرفعال است")
elif username == "admin" and password == "secret":  # = به == تغییر کرد
    print("خوش آمدید!")
else:
    print("اعتبارنامه نامعتبر")
```
</details>

---

## مرجع سریع

| عملگر | چه می‌کند | مثال |
|--------|----------|------|
| `==` | برابر با | `5 == 5` → `True` |
| `!=` | نابرابر با | `5 != 3` → `True` |
| `>` | بزرگتر از | `7 > 5` → `True` |
| `<` | کوچکتر از | `3 < 5` → `True` |
| `>=` | بزرگتر یا مساوی | `5 >= 5` → `True` |
| `<=` | کوچکتر یا مساوی | `4 <= 7` → `True` |
| `and` | هردو باید درست باشند | `True and True` → `True` |
| `or` | حداقل یکی درست باشد | `True or False` → `True` |
| `not` | معکوس می‌کند | `not True` → `False` |

---

## نکات کلیدی

1. **مقادیر بولی** `True` و `False` هستند
2. **مقایسه‌ها** (`== != > < >= <=`) True/False ایجاد می‌کنند
3. **AND** (`and`) نیاز دارد هردو شرط درست باشند
4. **OR** (`or`) نیاز دارد حداقل یک شرط درست باشد
5. **NOT** (`not`) True را به False و برعکس تبدیل می‌کند
6. **`==` مقایسه می‌کند**، **`=` منتسب می‌کند** - آنها را اشتباه نگیرید!

---

## گام بعدی

حالا درست و نادرست را می‌فهمید! در ادامه یاد می‌گیریم:
- چگونه شرایط را تودرتو کنیم (شرایط داخل شرایط)
- چگونه با حلقه‌ها کد را تکرار کنیم
- چگونه با توابع کد را سازماندهی کنیم
