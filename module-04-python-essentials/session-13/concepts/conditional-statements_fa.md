# دستورات شرطی: تصمیم‌گیری در کد

## چه چیزهایی یاد خواهید گرفت
- چگونه برنامه خود را تصمیم‌گیرنده کنید
- استفاده از دستورات `if`، `else` و `elif`
- مقایسه مقادیر برای انتخاب
- ترکیب شرایط متعدد
- اشتباهات رایج برای اجتناب

---

## مفهوم اصلی: برنامه مسیر خود را انتخاب می‌کند

دستور شرطی را مثل یک چهارراه در نظر بگیرید. برنامه شما به یک نقطه تصمیم می‌رسد و بر اساس یک شرط مسیر خود را انتخاب می‌کند.

**مثال: چراغ راهنمایی**
- چراغ سبز؟ → برو!
- چراغ قرمز؟ → بایست!
- چراغ زرد؟ → آهسته برو و آماده توقف شو.

---

## دستور if پایه

### تصمیم‌گیری ساده

```python
سن = 20

if سن >= 18:
    print("تو بزرگسال هستی!")
    print("می‌توانی رای بدهی.")

print("این همیشه چاپ می‌شود (خارج از if)")
```

**نحوه کار:**
۱. بررسی می‌کند آیا `سن >= 18` درست است (`True`)
۲. اگر بله، کد تورفتگی را اجرا می‌کند
۳. خط آخر همیشه اجرا می‌شود

### ساختار if-else

```python
دما = 85

if دما > 80:
    print("هوا گرم است!")
    print("شلوارک بپوش.")
else:
    print("هوا خیلی گرم نیست.")
    print("لباس معمولی خوب است.")
```

**نکته کلیدی:** یکی از این‌ها همیشه اجرا می‌شود—یا بخش `if` یا بخش `else`.

### چند انتخاب با elif

```python
نمره = 85

if نمره >= 90:
    نتیجه = "عالی"
    پیام = "کار فوق‌العاده!"
elif نمره >= 80:
    نتیجه = "خوب"
    پیام = "کار خوبی کردی!"
elif نمره >= 70:
    نتیجه = "متوسط"
    پیام = "عملکرد مناسب."
elif نمره >= 60:
    نتیجه = "ضعیف"
    پیام = "نیاز به بهبود دارد."
else:
    نتیجه = "مردود"
    پیام = "مردود شدی."

print(f"نتیجه شما: {نتیجه} - {پیام}")
```

**نحوه کار elif:**
- پایتون هر شرط را به ترتیب بررسی می‌کند
- وقتی یک شرط `True` پیدا کرد، آن بخش را اجرا و بقیه را رد می‌کند
- `else` هر چیزی که مطابقت نداشت را می‌گیرد

---

## عملگرهای مقایسه در شرایط

### مقایسه‌های پایه

```python
# بررسی برابری
نام = "علی"
if نام == "علی":
    print("سلام، علی!")

# بررسی نابرابری
رمز = "secret123"
if رمز != "admin":
    print("رمز پیش‌فرض نیست. خوب!")

# بزرگ‌تر و کوچک‌تر
سن = 25
if سن > 18:
    print("بزرگسال")
if سن < 13:
    print("کودک")

# بزرگ‌تر/کوچک‌تر یا مساوی
اقلام = 5
if اقلام >= 5:
    print("اقلام کافی داری")
```

### بررسی «درستی» (Truthy)

```python
# رشته‌های غیرخالی «درست» هستند
نام = input("نام خود را وارد کن: ")
if نام:  # مثل: if نام != ""
    print(f"سلام، {نام}!")
else:
    print("نامی وارد نکردی.")

# اعداد غیرصفر «درست» هستند
تعداد = 0
if تعداد:
    print(f"{تعداد} پیام داری")
else:
    print("پیامی نداری")
```

---

## ترکیب شرایط با عملگرهای منطقی

### استفاده از AND (هر دو باید درست باشند)

```python
سن = 20
گواهینامه_دارد = True

# هر دو شرط باید درست باشند
if سن >= 18 and گواهینامه_دارد:
    print("می‌توانی رانندگی کنی!")
else:
    print("نمی‌توانی رانندگی کنی.")
```

### استفاده از OR (حداقل یکی باید درست باشد)

```python
روز = "شنبه"
تعطیل_رسمی = False

# هر کدام از شرایط می‌تواند درست باشد
if روز == "شنبه" or روز == "یکشنبه" or تعطیل_رسمی:
    print("روز تعطیل است!")
else:
    print("روز کاری است.")
```

### استفاده از NOT (معکوس شرط)

```python
باران_می‌بارد = False

if not باران_می‌بارد:
    print("چتر لازم نیست!")
else:
    print("چتر ببر.")

# مثال دیگر
وارد_شده = False
if not وارد_شده:
    print("لطفاً اول وارد شو.")
```

### ترکیب همه سه

```python
سن = 25
بلیط_دارد = True
vip_هست = False

# شرط پیچیده
if (سن >= 18 and بلیط_دارد) or vip_هست:
    print("به کنسرت خوش آمدید!")
else:
    print("نمی‌توانید وارد شوید.")
```

---

## اشتباهات رایج مبتدی‌ها

### اشتباه ۱: استفاده از `=` به جای `==`

```python
# ❌ اشتباه - این تلاش برای مقداردهی است
سن = 25
if سن = 18:  # SyntaxError!
    print("۱۸ سال داری")

# ✅ درست - مقایسه مقادیر
if سن == 18:
    print("دقیقاً ۱۸ سال داری")
```

### اشتباه ۲: فراموش کردن دو نقطه

```python
# ❌ اشتباه - دو نقطه گم شده
if سن > 18
    print("بزرگسال")

# ✅ درست
if سن > 18:
    print("بزرگسال")
```

### اشتباه ۳: تورفتگی اشتباه

```python
# ❌ اشتباه - تورفتگی ناسازگار
if سن > 18:
    print("بزرگسال")
     print("می‌تواند رای دهد")  # خطای تورفتگی!

# ✅ درست - ۴ فاصله استفاده کنید
if سن > 18:
    print("بزرگسال")
    print("می‌تواند رای دهد")
```

### اشتباه ۴: اشتباه گرفتن elif با چند if

```python
# ❌ اشتباه - چند if (همه ممکن است اجرا شوند)
نمره = 95
if نمره >= 60:
    print("قبول")
if نمره >= 80:
    print("خوب")
if نمره >= 90:
    print("عالی")
# این هر سه را چاپ می‌کند!

# ✅ درست - elif (فقط یکی اجرا می‌شود)
if نمره >= 90:
    print("عالی")
elif نمره >= 80:
    print("خوب")
elif نمره >= 60:
    print("قبول")
```

### اشتباه ۵: حساسیت بزرگی و کوچکی حروف در مقایسه رشته

```python
# ❌ اشتباه - حروف مهم هستند!
نام = "علی"
if نام == "Ali":
    print("سلام!")  # چاپ نمی‌شود!

# ✅ درست - هر دو حالت را چک کنید یا تبدیل کنید
if نام == "Ali" or نام == "علی":
    print("سلام!")

# یا بهتر:
if نام.lower() == "علی":
    print("سلام!")
```

---

## خودتان امتحان کنید: تمرین‌ها

### تمرین ۱: دسته‌بندی سن

افراد را بر اساس سن دسته‌بندی کنید.

```python
سن = int(input("سن خود را وارد کن: "))

if سن < 0:
    print("سن نامعتبر!")
elif سن < 13:
    print("تو کودک هستی.")
elif سن < 20:
    print("تو نوجوان هستی.")
elif سن < 65:
    print("تو بزرگسال هستی.")
else:
    print("تو سالمند هستی.")
```

### تمرین ۲: سیستم ورود ساده

نام کاربری و رمز عبور را بررسی کنید.

```python
# اطلاعات ذخیره شده
نام_صحیح = "admin"
رمز_صحیح = "secret123"

# گرفتن ورودی کاربر
نام = input("نام کاربری: ")
رمز = input("رمز عبور: ")

# بررسی اطلاعات
if نام == نام_صحیح and رمز == رمز_صحیح:
    print("ورود موفق! خوش آمدید!")
elif نام != نام_صحیح:
    print("خطا: نام کاربری یافت نشد.")
elif رمز != رمز_صحیح:
    print("خطا: رمز عبور اشتباه است.")
```

### تمرین ۳: بازی حدس عدد

راهنمایی درباره یک عدد بدهید.

```python
import random

عدد_مخفی = random.randint(1, 100)
حدس = int(input("یک عدد حدس بزن (۱-۱۰۰): "))

if حدس == عدد_مخفی:
    print("🎉 درست است! برنده شدی!")
elif حدس < عدد_مخفی:
    print("کم است! دوباره امتحان کن.")
else:
    print("زیاد است! دوباره امتحان کن.")

print(f"عدد مخفی بود: {عدد_مخفی}")
```

### تمرین ۴: رفع اشکالات

خطاها را پیدا و رفع کنید:

```python
# برنامه با اشکال
سن = input("چند سال داری؟ ")

if سن > 18
    print("بزرگسال")
    print("می‌توانی رای بدهی")
else
    print("صغیر")

if سن = 18
    print("دقیقاً ۱۸!")
```

<details>
<summary>برای دیدن پاسخ کلیک کنید</summary>

```python
# برنامه رفع شده
سن = int(input("چند سال داری؟ "))  # تبدیل به عدد!

if سن > 18:  # دو نقطه اضافه شد
    print("بزرگسال")
    print("می‌توانی رای بدهی")
else:  # دو نقطه اضافه شد
    print("صغیر")

if سن == 18:  # از == استفاده کنید و دو نقطه بگذارید
    print("دقیقاً ۱۸!")
```
</details>

---

## مرجع سریع

### ساختار شرط

```python
# if ساده
if شرط:
    # کد

# if-else
if شرط:
    # کد اگر درست باشد
else:
    # کد اگر غلط باشد

# if-elif-else
if شرط۱:
    # کد ۱
elif شرط۲:
    # کد ۲
elif شرط۳:
    # کد ۳
else:
    # کد پیش‌فرض
```

### مقایسه‌های رایج

| عملگر | معنی | مثال | نتیجه |
|--------|--------|--------|--------|
| `==` | برابر با | `5 == 5` | `True` |
| `!=` | نابرابر با | `5 != 3` | `True` |
| `>` | بزرگ‌تر از | `7 > 5` | `True` |
| `<` | کوچک‌تر از | `3 < 5` | `True` |
| `>=` | بزرگ‌تر یا مساوی | `5 >= 5` | `True` |
| `<=` | کوچک‌تر یا مساوی | `4 <= 5` | `True` |

### عملگرهای منطقی

| عملگر | معنی | مثال | نتیجه |
|--------|--------|--------|--------|
| `and` | هر دو درست | `True and True` | `True` |
| `or` | حداقل یکی درست | `True or False` | `True` |
| `not` | معکوس | `not True` | `False` |

### قوانین کلیدی

| قانون | مثال |
|--------|------|
| همیشه از `==` برای مقایسه استفاده کنید | `if x == 5:` |
| دو نقطه را فراموش نکنید | `if x > 5:` |
| بلوک کد را تورفتگی بدهید | ۴ فاصله |
| `elif` یعنی «else if» | `elif x > 3:` |
| `else` شرط ندارد | `else:` |

---

## نکات کلیدی

۱. **دستورات `if`** به برنامه اجازه تصمیم‌گیری می‌دهند
۲. **`else`** جایگزینی برای وقتی شرط غلط است فراهم می‌کند
۳. **`elif`** (else if) اجازه می‌دهد چند شرط به ترتیب بررسی شوند
۴. **از `==`** برای مقایسه استفاده کنید، نه `=` (که مقدار می‌دهد)
۵. **دو نقطه را فراموش نکنید** `:` در انتهای خطوط if/elif/else
۶. **تورفتگی مهم است**—پایتون از آن برای تشخیص کد بلوک استفاده می‌کند
۷. **شرایط را با** `and`، `or` و `not` ترکیب کنید

---

## گام بعدی

حالا که می‌توانید تصمیم بگیرید:
- درباره منطق بولی یاد می‌گیرید (جزئیات مقادیر درست/غلط)
- درخت‌های تصمیم پیچیده‌تر می‌نویسید (شرایط تو در تو)
- شرایط را با حلقه‌ها ترکیب می‌کنید
