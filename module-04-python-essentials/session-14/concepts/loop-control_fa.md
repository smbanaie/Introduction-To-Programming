# کنترل حلقه: break، continue و else

## چه چیزی یاد خواهی گرفت
- چگونه زودتر از حلقه خارج شویم با `break`
- چگونه یک تکرار را رد کنیم با `continue`
- چگونه از `else` با حلقه استفاده کنیم
- کی از `pass` به‌عنوان placeholder استفاده کنیم
- اشتباهات رایج مبتدی‌ها

---

## کنترل جریان حلقه

گاهی باید نحوه رفتار حلقه را تغییر دهی:
- متوقف کردن زودهنگام حلقه وقتی چیزی را پیدا کردی
- رد کردن بعضی آیتم‌ها که مطابقت ندارند
- اجرای کد وقتی حلقه به‌طور عادی تمام شد (بدون break)

---

## break: فوراً حلقه را متوقف کن

`break` فوراً از حلقه خارج می‌شود و تکرارهای باقی‌مانده را نادیده می‌گیرد.

### تشبیه واقعی: خارج شدن از ساختمان

```
داری اتاق به اتاق دنبال کلید می‌گردی.
اتاق ۳ پیداش کردی! 🎉
نیازی به چک کردن اتاق‌های ۴، ۵، ۶... نیست
فوراً از ساختمان خارج شو!
```

### استفاده از break در حلقه For

```python
# اولین عدد زوج را پیدا کن و متوقف شو
numbers = [1, 3, 5, 8, 9, 10, 12]

for num in numbers:
    if num % 2 == 0:  # اگر زوج است
        print(f"عدد زوج پیدا شد: {num}")
        break  # فوراً متوقف شو!

print("جستجو تمام شد.")
```

**خروجی:**
```
عدد زوج پیدا شد: 8
جستجو تمام شد.
```

توجه کن که ۱۰ و ۱۲ را چک نکرد - در اولین تطابق متوقف شد!

### استفاده از break در حلقه While

```python
# تا وقتی درست حدس نزدی، حدس بزن
secret = 7

while True:  # تا ابد...
    guess = int(input("عدد را حدس بزن (۱-۱۰): "))
    
    if guess == secret:
        print("درست! 🎉")
        break  # خروج از حلقه
    
    print("دوباره امتحان کن!")

print("ممنون بابت بازی!")
```

---

## continue: برو به تکرار بعدی

`continue` بقیه تکرار فعلی را رد می‌کند و به تکرار بعدی می‌رود.

### تشبیه واقعی: رد کردن یک مرحله در خط تولید

```
داری سیب‌های روی تسمه نقاله را چک می‌کنی.
سیب خوب → بسته‌بندی
سیب بد → رد کن (continue)
برو به سیب بعدی
```

### استفاده از continue در حلقه

```python
# فقط اعداد زوج را چاپ کن، فردها را رد کن
for num in range(1, 11):
    if num % 2 != 0:  # اگر فرد است
        continue  # این را رد کن
    
    print(f"عدد زوج: {num}")

print("تمام!")
```

**خروجی:**
```
عدد زوج: 2
عدد زوج: 4
عدد زوج: 6
عدد زوج: 8
عدد زوج: 10
تمام!
```

### مثال دیگر: رد کردن ورودی نامعتبر

```python
# فقط نمرات معتبر را پردازش کن
scores = [85, -5, 92, 0, 78, -3, 95]

for score in scores:
    if score < 0:
        print(f"رد کردن نمره نامعتبر: {score}")
        continue
    
    print(f"نمره معتبر: {score}")

print("همه نمرات پردازش شدند.")
```

**خروجی:**
```
نمره معتبر: 85
رد کردن نمره نامعتبر: -5
نمره معتبر: 92
نمره معتبر: 0
نمره معتبر: 78
رد کردن نمره نامعتبر: -3
نمره معتبر: 95
همه نمرات پردازش شدند.
```

---

## نمودار ASCII: نحوه کار break و continue

```
جریان عادی حلقه:
┌─────────────┐
│   شروع     │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   چک       │
│   شرط      │
└──────┬──────┘
       │
   ┌───┴───┐
   │  درست │ نادرست
   │       ├────────► تمام
   ▼       │
┌─────────────┐
│ بدنه حلقه  │
│  (کار)     │
└──────┬──────┘
       │
       └──────────► برگشت به چک

با break:
بدنه حلقه
   │
   ├───► اگر شرط ───► break ────► فوراً خارج شو!
   │         │
   │         │ نادرست
   │         ▼
   └───► ادامه کار

با continue:
بدنه حلقه
   │
   ├───► اگر شرط ───► continue ──► برو به تکرار بعدی
   │         │
   │         │ نادرست
   │         ▼
   └───► ادامه کار
```

---

## else با حلقه: اجرا وقتی حلقه به‌طور عادی تمام شد

بلوک `else` وقتی اجرا می‌شود که حلقه به‌طور عادی تمام شود - یعنی به `break` نخورده باشد.

### کی else اجرا می‌شود و کی نمی‌شود

```python
# مثال ۱: else اجرا می‌شود چون حلقه تکمیل شد
for i in range(3):
    print(i)
else:
    print("حلقه به‌طور عادی تکمیل شد! ✅")

# خروجی:
# 0
# 1
# 2
# حلقه به‌طور عادی تکمیل شد! ✅
```

```python
# مثال ۲: else اجرا نمی‌شود چون break خوردیم
for i in range(3):
    print(i)
    if i == 1:
        break
else:
    print("حلقه به‌طور عادی تکمیل شد! ✅")

# خروجی:
# 0
# 1
# (بلوک else اجرا نمی‌شود!)
```

### تشبیه واقعی: گنج‌یابی

```python
rooms = ["آشپزخانه", "اتاق خواب", "کتابخانه"]
treasure_room = "کتابخانه"

for room in rooms:
    print(f"دنبال گنج در {room}...")
    if room == treasure_room:
        print(f"🏆 گنج در {room} پیدا شد!")
        break
else:
    # این فقط اگر گنج پیدا نشده اجرا می‌شود!
    print("😢 گنجی پیدا نشد.")
```

**خروجی:**
```
دنبال گنج در آشپزخانه...
دنبال گنج در اتاق خواب...
دنبال گنج در کتابخانه...
🏆 گنج در کتابخانه پیدا شد!
```

`else` اجرا نمی‌شود چون گنج را پیدا کردیم!

### استفاده عملی: جستجو با پیام پیدا نشد

```python
# جستجو برای یک نام در لیست
names = ["علی", "بابک", "چارلی"]
search_name = "داوود"

for name in names:
    if name == search_name:
        print(f"{name} پیدا شد!")
        break
else:
    print(f"{search_name} در لیست نیست.")

# خروجی: داوود در لیست نیست.
```

---

## pass: placeholder که هیچ کاری نمی‌کند

`pass` یک placeholder است وقتی باید کدی بنویسی ولی هنوز نمی‌خواهی کاری انجام بدهی.

### کی از pass استفاده کنی

```python
# داری یک تابع را برنامه‌ریزی می‌کنی ولی هنوز ننوشتی
def future_function():
    pass  # TODO: بعداً پیاده‌سازی شود

# در یک حلقه که ممکن است بعداً کد اضافه کنی
for item in items:
    if item.is_special():
        pass  # TODO: آیتم‌های خاص را مدیریت کن
    else:
        print(item)
```

`pass` واقعاً هیچ کاری نمی‌کند - فقط برای این است که Python از بلوک خالی شکایت نکند.

---

## اشتباهات رایج مبتدی‌ها

### اشتباه ۱: استفاده از break به جای continue

```python
# ❌ اشتباه - کاملاً متوقف می‌شود به جای فقط رد کردن
for num in range(10):
    if num == 5:
        break  # کل حلقه متوقف می‌شود!
    print(num)
# فقط چاپ می‌کند: ۰، ۱، ۲، ۳، ۴

# ✅ درست - فقط این عدد را رد می‌کند
for num in range(10):
    if num == 5:
        continue  # فقط ۵ را رد می‌کند
    print(num)
# چاپ می‌کند: ۰، ۱، ۲، ۳، ۴، ۶، ۷، ۸، ۹
```

### اشتباه ۲: فراموش کردن break در While True

```python
# ❌ اشتباه - حلقه بی‌نهایت!
while True:
    answer = input("ادامه بده؟ (بله/خیر): ")
    if answer == "خیر":
        print("خداحافظ!")
        # فراموش کردم break! حلقه تا ادامه دارد!

# ✅ درست - همیشه خارج شو
while True:
    answer = input("ادامه بده؟ (بله/خیر): ")
    if answer == "خیر":
        print("خداحافظ!")
        break  # خروج از حلقه
```

### اشتباه ۳: فکر کردن که else همیشه بعد از break اجرا می‌شود

```python
# ❌ درک اشتباه
for i in range(5):
    if i == 3:
        break
else:
    print("تمام!")  # ❌ این چاپ نمی‌شود!

# ✅ else فقط اجرا می‌شود اگر حلقه بدون break تمام شده
```

### اشتباه ۴: استفاده از continue در انتهای حلقه

```python
# ❌ بی‌فایده - اصلاً چیزی بعدش نیست
for i in range(5):
    print(i)
    continue  # اضافی - حلقه خودش ادامه می‌دهد!

# ✅ فقط وقتی continue استفاده کن که می‌خواهی کد بقیه را رد کنی
for i in range(10):
    if i % 2 == 0:
        continue  # اعداد زوج را رد کن
    print(i)  # فقط اعداد فرد را چاپ می‌کند
```

### اشتباه ۵: اشتباه گرفتن break در حلقه‌های تو در تو

```python
# ⚠️ break فقط از حلقه داخلی خارج می‌شود!
for i in range(3):
    for j in range(3):
        if j == 1:
            break  # فقط از حلقه داخلی خارج می‌شود!
        print(f"i={i}, j={j}")

# خروجی:
# i=0, j=0
# i=1, j=0
# i=2, j=0
```

---

## مثال‌های عملی

### مثال ۱: رمز عبور با حداکثر تلاش

```python
password = "secret123"
max_attempts = 3

for attempt in range(1, max_attempts + 1):
    guess = input(f"تلاش {attempt}/{max_attempts}: رمز را وارد کن: ")
    
    if guess == password:
        print("✅ دسترسی مجاز!")
        break
else:
    print("❌ تلاش‌های ناموفق زیاد. حساب قفل شد.")
```

### مثال ۲: پردازش داده با اعتبارسنجی

```python
data = [10, 25, -5, 30, "نامعتبر", 15, -3, 40]
valid_sum = 0
count = 0

for item in data:
    if not isinstance(item, int):  # رد کردن غیرعددها
        print(f"رد کردن غیرعددی: {item}")
        continue
    
    if item < 0:  # رد کردن منفی‌ها
        print(f"رد کردن منفی: {item}")
        continue
    
    valid_sum += item
    count += 1

print(f"مجموع {count} آیتم معتبر: {valid_sum}")
```

### مثال ۳: سیستم منو با break

```python
while True:
    print("\n=== منو ===")
    print("۱. افزودن آیتم")
    print("۲. مشاهده آیتم‌ها")
    print("۳. خروج")
    
    choice = input("انتخاب کن (۱-۳): ")
    
    if choice == "1":
        print("افزودن آیتم...")
    elif choice == "2":
        print("مشاهده آیتم‌ها...")
    elif choice == "3":
        print("خداحافظ!")
        break
    else:
        print("انتخاب نامعتبر. دوباره امتحان کن.")
```

---

## تمرین‌های «امتحان کن»

### تمرین ۱: پیدا کردن اولین بخش‌پذیر

اولین عدد بخش‌پذیر بر ۳ و ۵ بین ۱ تا ۵۰ را پیدا کن.

```python
for num in range(1, 51):
    if num % 3 == 0 and num % 5 == 0:
        print(f"پیدا شد: {num}")
        break
```

### تمرین ۲: جمع با رد کردن

جمع اعداد ۱ تا ۱۰، ولی مضارب ۳ را رد کن.

```python
total = 0
for num in range(1, 11):
    if num % 3 == 0:
        continue
    total += num

print(f"جمع (با رد کردن مضارب ۳): {total}")  # باید ۳۷ شود
```

### تمرین ۳: جستجو با else

در لیست به دنبال عددی بگرد. اگر پیدا نشد، پیام بده.

```python
numbers = [10, 20, 30, 40, 50]
target = 25

# کد خود را اینجا بنویس
for num in numbers:
    if num == target:
        print(f"{target} پیدا شد!")
        break
else:
    print(f"{target} در لیست نیست.")
```

---

## مرجع سریع

| دستور | چه کاری انجام می‌دهد | کی استفاده کنی |
|-----------|--------------|-------------|
| `break` | فوراً از حلقه خارج می‌شود | چیزی را که می‌خواستی پیدا کردی، جستجو را متوقف کن |
| `continue` | بقیه تکرار فعلی را رد می‌کند | این آیتم را رد کن، با بقیه ادامه بده |
| `else` | فقط اگر حلقه به‌طور عادی تمام شد اجرا می‌شود | کاری انجام بده اگر جستجو ناموفق بود |
| `pass` | هیچ کاری نمی‌کند | placeholder برای کد آینده |

---

## نکات کلیدی

۱. **`break`** - فوراً کل حلقه را متوقف می‌کند
۲. **`continue`** - بقیه تکرار فعلی را رد می‌کند و به بعدی می‌رود
۳. **`else`** - فقط اگر حلقه بدون break تمام شده باشد اجرا می‌شود
۴. **`pass`** - هیچ کاری نمی‌کند، فقط placeholder است
۵. از `break` وقتی **چیزی را پیدا کردی** استفاده کن
۶. از `continue` وقتی می‌خواهی **بعضی آیتم‌ها را رد کنی**
۷. از `else` برای تشخیص **وقتی جستجو چیزی پیدا نکرد** استفاده کن

---

## قدم بعدی چیه؟

حالا می‌دانی چگونه حلقه‌ها را کنترل کنی! بعدی:
- List comprehensions (ساخت لیست با حلقه)
- حلقه‌های تو در تو (حلقه داخل حلقه)
- الگوهای پیچیده‌تر حلقه
