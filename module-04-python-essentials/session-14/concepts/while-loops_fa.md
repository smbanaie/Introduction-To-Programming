# حلقه‌های while: تکرار در حالی که شرط درست است

## چه چیزهایی یاد خواهید گرفت
- چگونه از حلقه‌های while برای تکرار ناشناخته استفاده کنید
- تفاوت بین for و while
- الگوهای رایج حلقه while
- چگونه از حلقه‌های بی‌نهایت اجتناب کنید
- اعتبارسنجی ورودی با حلقه‌های while

---

## مفهوم اصلی: ادامه بده در حالی که...

یک **حلقه while** کد را تا زمانی که یک شرط `True` باقی می‌ماند تکرار می‌کند. برخلاف حلقه‌های for که در یک لیست می‌روند، حلقه‌های while تا زمانی که چیزی تغییر کند ادامه می‌یابند.

**مثال: شستن دست‌ها**
- در حالی که دست‌ها کثیف هستند: به شستن ادامه بده
- وقتی دست‌ها تمیز شدند: متوقف شو
- نمی‌دانید چند بار باید بشویید!

---

## سینتکس پایه حلقه while

### شمارنده ساده

```python
شمارنده = 1

while شمارنده <= 5:
    print(f"شمارش: {شمارنده}")
    شمارنده = شمارنده + 1  # یا: شمارنده += 1

print("تمام!")

# خروجی:
# شمارش: 1
# شمارش: 2
# شمارش: 3
# شمارش: 4
# شمارش: 5
# تمام!
```

**نحوه کار:**
۱. بررسی می‌کند آیا `شمارنده <= 5` → درست (۱ <= ۵)
۲. کد تورفتگی را اجرا می‌کند
۳. شمارنده را ۱ زیاد می‌کند
۴. دوباره بررسی می‌کند → درست (۲ <= ۵)
۵. تکرار تا شمارنده ۶ شود
۶. ۶ <= ۵ نادرست است، پس حلقه تمام می‌شود

### شرط بولی

```python
ادامه_بده = True

while ادامه_بده:
    پاسخ = input("ادامه می‌دهید؟ (بله/خیر): ").lower()
    
    if پاسخ == "خیر":
        ادامه_بده = False
    else:
        print("ادامه می‌دهیم...")

print("خداحافظ!")
```

---

## الگوهای رایج حلقه while

### الگوی ۱: اعتبارسنجی ورودی

دریافت ورودی معتبر از کاربر:

```python
# تا زمانی که کاربر عدد مثبت وارد نکرد ادامه بده
عدد = 0  # شروع با مقدار نامعتبر

while عدد <= 0:
    try:
        عدد = float(input("یک عدد مثبت وارد کن: "))
        if عدد <= 0:
            print("مثبت نبود! دوباره امتحان کن.")
    except ValueError:
        print("این عدد نیست! دوباره امتحان کن.")
        عدد = 0  # ریست برای ادامه حلقه

print(f"ممنون! عدد وارد شده: {عدد}")
```

### الگوی ۲: مقدار Sentinel (مقدار خاص توقف)

```python
# تا زمانی که کاربر ۰ وارد نکرد به خواندن اعداد ادامه بده
مجموع = 0

print("اعداد را برای جمع وارد کن (۰ برای توقف):")

while True:
    عدد = int(input("عدد: "))
    
    if عدد == 0:
        break  # خروج از حلقه
    
    مجموع = مجموع + عدد
    print(f"اضافه شد. مجموع فعلی: {مجموع}")

print(f"مجموع نهایی: {مجموع}")
```

### الگوی ۳: سیستم منو

```python
def نمایش_منو():
    print("\n=== ماشین‌حساب ===")
    print("۱. جمع")
    print("۲. تفریق")
    print("۳. ضرب")
    print("۴. تقسیم")
    print("۵. خروج")

while True:
    نمایش_منو()
    انتخاب = input("انتخاب کن (۱-۵): ")
    
    if انتخاب == "5":
        print("خداحافظ!")
        break
    
    if انتخاب in ["1", "2", "3", "4"]:
        try:
            a = float(input("عدد اول: "))
            b = float(input("عدد دوم: "))
            
            if انتخاب == "1":
                print(f"نتیجه: {a + b}")
            elif انتخاب == "2":
                print(f"نتیجه: {a - b}")
            elif انتخاب == "3":
                print(f"نتیجه: {a * b}")
            elif انتخاب == "4":
                if b != 0:
                    print(f"نتیجه: {a / b}")
                else:
                    print("نمی‌توان بر صفر تقسیم کرد!")
        except ValueError:
            print("عدد نامعتبر!")
    else:
        print("انتخاب نامعتبر!")
```

---

## حلقه‌های بی‌نهایت و نحوه اجتناب از آن‌ها

### حلقه بی‌نهایت تصادفی

```python
# ❌ اشتباه - این برای همیشه اجرا می‌شود!
شمارنده = 1

while شمارنده <= 5:
    print(شمارنده)
    # شمارنده += 1 را فراموش کردیم!

# خروجی: ۱، ۱، ۱، ۱، ۱... برای همیشه
```

### نحوه رفع آن

```python
# ✅ درست - این درست متوقف می‌شود
شمارنده = 1

while شمارنده <= 5:
    print(شمارنده)
    شمارنده += 1  # این را فراموش نکنید!
```

### حلقه بی‌نهایت عمدی با break

```python
# گاهی اوقات حلقه‌های بی‌نهایت مفید هستند
while True:
    دستور = input("دستور را وارد کن (خروج برای پایان): ")
    
    if دستور.lower() == "خروج":
        print("ممنون از بازی!")
        break  # خروج از حلقه
    
    print(f"در حال اجرا: {دستور}")
```

---

## حلقه‌های for در مقابل while

### از حلقه for وقتی تعداد را می‌دانید استفاده کنید

```python
# ✅ خوب - دقیقاً ۱۰ تکرار می‌دانیم
for i in range(10):
    print(f"آیتم {i}")

# ✅ خوب - یک لیست برای پردازش داریم
for میوه in ["سیب", "موز", "گیلاس"]:
    print(میوه)
```

### از حلقه while وقتی تعداد را نمی‌دانید استفاده کنید

```python
# ✅ خوب - نمی‌دانیم چند تلاش نیاز است
رمز = ""
while رمز != "secret":
    رمز = input("رمز را وارد کن: ")

# ✅ خوب - به ورودی کاربر بستگی دارد
خطوط = []
خط = input("خط را وارد کن (خالی برای پایان): ")
while خط != "":
    خطوط.append(خط)
    خط = input("خط را وارد کن (خالی برای پایان): ")
```

---

## اشتباهات رایج مبتدی‌ها

### اشتباه ۱: فراموش کردن به‌روزرسانی متغیر شرط

```python
# ❌ اشتباه - حلقه بی‌نهایت
شمارنده = 1
while شمارنده <= 5:
    print(شمارنده)
    # شمارنده += 1 را فراموش کردیم!

# ✅ درست
شمارنده = 1
while شمارنده <= 5:
    print(شمارنده)
    شمارنده += 1
```

### اشتباه ۲: جهت مقایسه اشتباه

```python
# ❌ اشتباه - هرگز وارد حلقه نمی‌شود
عدد = 10
while عدد > 20:  # ۱۰ > ۲۰ بلافاصله نادرست است!
    print(عدد)
    عدد += 1

# ✅ درست
عدد = 10
while عدد < 20:  # ۱۰ < ۲۰ درست است
    print(عدد)
    عدد += 1
```

### اشتباه ۳: خطای کم/زیاد یکی

```python
# ❌ اشتباه - ۱ تا ۱۱ را چاپ می‌کند (۱۱ بار، نه ۱۰)
شمارنده = 1
while شمارنده <= 10:
    print(شمارنده)
شمارنده += 1  # این خارج از حلقه است!

# ✅ درست - تورفتگی صحیح
شمارنده = 1
while شمارنده <= 10:
    print(شمارنده)
    شمارنده += 1  # این داخل حلقه است
```

### اشتباه ۴: مقداردهی اولیه نکردن متغیر

```python
# ❌ اشتباه - شمارنده چیست؟
while شمارنده <= 5:  # NameError!
    print(شمارنده)
    شمارنده += 1

# ✅ درست - ابتدا مقداردهی کن
شمارنده = 1
while شمارنده <= 5:
    print(شمارنده)
    شمارنده += 1
```

---

## خودتان امتحان کنید: تمرین‌ها

### تمرین ۱: بازی حدس عدد

تا درست حدس زدن ادامه بده:

```python
import random

عدد_مخفی = random.randint(1, 100)
حدس‌ها = 0

حدس = 0  # مقداردهی اولیه با مقدار غیرممکن

while حدس != عدد_مخفی:
    حدس = int(input("حدس بزن (۱-۱۰۰): "))
    حدس‌ها += 1
    
    if حدس < عدد_مخفی:
        print("کم است!")
    elif حدس > عدد_مخفی:
        print("زیاد است!")

print(f"🎉 درست است! در {حدس‌ها} حدس درست زدی!")
```

### تمرین ۲: تلاش‌های رمز عبور

محدود کردن تعداد تلاش‌ها:

```python
رمز_صحیح = "python123"
تلاش‌ها = 0
حداکثر_تلاش = 3

while تلاش‌ها < حداکثر_تلاش:
    رمز = input("رمز را وارد کن: ")
    تلاش‌ها += 1
    
    if رمز == رمز_صحیح:
        print("دسترسی مجاز!")
        break
    else:
        باقیمانده = حداکثر_تلاش - تلاش‌ها
        if باقیمانده > 0:
            print(f"اشتباه! {باقیمانده} تلاش باقی مانده.")
        else:
            print("حساب قفل شد!")
```

### تمرین ۳: میانگین با مقدار Sentinel

اضافه کردن اعداد تا وارد کردن -۱:

```python
مجموع = 0
شمارنده = 0

print("اعداد را برای میانگین وارد کن (-۱ برای پایان):")

عدد = 0
while عدد != -1:
    عدد = float(input("عدد: "))
    
    if عدد != -1:
        مجموع += عدد
        شمارنده += 1

if شمارنده > 0:
    میانگین = مجموع / شمارنده
    print(f"شما {شمارنده} عدد وارد کردید.")
    print(f"جمع: {مجموع}")
    print(f"میانگین: {میانگین:.2f}")
else:
    print("هیچ عددی وارد نشد.")
```

### تمرین ۴: رفع اشکالات

```python
# برنامه با اشکال
شمارنده = 1
while شمارنده < 5
    print(شمارنده)
شمارنده += 1

رمز = ""
while رمز == "secret"
    رمز = input("رمز: ")
print("دسترسی مجاز")
```

<details>
<summary>برای دیدن پاسخ کلیک کنید</summary>

```python
# برنامه رفع شده
شمارنده = 1
while شمارنده <= 5:  # دو نقطه اضافه شد
    print(شمارنده)
    شمارنده += 1  # تورفتگی برای داخل حلقه بودن

رمز = ""
while رمز != "secret":  # : لازم است و != (نه ==)
    رمز = input("رمز: ")
print("دسترسی مجاز")
```
</details>

---

## مرجع سریع

### حلقه while پایه

```python
while شرط:
    # کد برای تکرار
    # به‌روزرسانی متغیر شرط
```

### الگوهای رایج

| الگو | کد |
|------|-----|
| اعتبارسنجی ورودی | `while not معتبر:`<br>`  گرفتن_ورودی()`<br>`  اعتبارسنجی()` |
| حلقه منو | `while True:`<br>`  نمایش_منو()`<br>`  if خروج: break` |
| شمارش بالا | `شمارنده = 0`<br>`while شمارنده < حداکثر:`<br>`  کاری_انجام_بده()`<br>`  شمارنده += 1` |
| مقدار sentinel | `while True:`<br>`  مقدار = input()`<br>`  if مقدار == sentinel: break` |

### for در مقابل while

| استفاده | نوع حلقه |
|--------|-----------|
| تعداد تکرارهای مشخص | `for` |
| پردازش لیست/مجموعه | `for` |
| تکرارهای نامشخص | `while` |
| انتظار برای شرط | `while` |
| ورودی تا معتبر شدن | `while` |

### اجتناب از حلقه‌های بی‌نهایت

| بررسی | مثال |
|-------|---------|
| مقداردهی اولیه متغیر | `شمارنده = 0` قبل از `while شمارنده < 10` |
| به‌روزرسانی در حلقه | `شمارنده += 1` داخل حلقه |
| شرط می‌تواند نادرست شود | مطمئن شوید شمارنده نهایتاً به ۱۰ می‌رسد |
| استفاده از break برای خروج عمدی | `if تمام: break` |

---

## نکات کلیدی

۱. **حلقه‌های while** در حالی که شرط `True` است اجرا می‌شوند
۲. **حلقه‌های for** برای تکرارهای مشخص؛ **حلقه‌های while** برای نامشخص
۳. **به‌روزرسانی متغیر شرط** در داخل حلقه را فراموش نکنید
۴. **حلقه‌های بی‌نهایت** وقتی شرط هیچ‌وقت `False` نمی‌شود رخ می‌دهند
۵. **از `break`** برای خروج زودهنگام از حلقه استفاده کنید
۶. **اعتبارسنجی ورودی** یکی از موارد کلاسیک استفاده از while است
۷. **همیشه مقداردهی اولیه** متغیرها را قبل از حلقه while انجام دهید

---

## گام بعدی

حالا که هر دو حلقه for و while را می‌شناسید:
- درباره `break` و `continue` یاد می‌گیرید برای تنظیم دقیق حلقه‌ها
- درباره `else` با حلقه‌ها یاد می‌گیرید
- برنامه‌های تعاملی پیچیده‌تر می‌نویسید
